# Lõi Web Framework
fastapi==0.110.0
uvicorn[standard]==0.29.0

# Xử lý Form và File Upload (Dành cho việc upload file ZIP/PDF CV)
python-multipart==0.0.9

# Cấu hình Cơ sở dữ liệu (PostgreSQL)
sqlalchemy==2.0.29
psycopg2-binary==2.9.9
alembic==1.13.1

# Xác thực và Phân quyền (JWT & Passwords)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2

# Bỏ ghim cứng: package `mcp` (backend làm MCP client) cần pydantic/pydantic-settings
# mới hơn, xung đột với các bản ghim cũ. Để pip tự giải bộ tương thích.
fastapi-mail

# Xác thực dữ liệu và Biến môi trường
pydantic[email]>=2.7.2
pydantic-settings>=2.5.2

pymupdf==1.24.0
google-generativeai==0.8.3
groq==0.11.0
# groq 0.11 gọi httpx theo kiểu cũ (tham số proxies); httpx>=0.28 đã bỏ -> ghim bản tương thích.
httpx==0.27.2
python-dotenv==1.0.1

# Task queue nền: xử lý/chấm điểm CV bất đồng bộ, không chặn request upload
celery[redis]==5.3.6
redis==5.0.3

# Backend làm MCP CLIENT: agent loop gọi tool qua MCP server (Streamable HTTP) thay
# vì gọi thẳng hàm Python -> MCP thực sự nằm trong luồng chạy của web.
#
# GHIM khoảng phiên bản: SDK này còn đổi hành vi nhanh. Từ 1.10, FastMCP sinh
# outputSchema từ annotation trả về và VALIDATE kết quả — đủ để làm hỏng những tool
# vốn chạy tốt ở bản cũ. Để trôi tự do thì một lần `docker build` lại là một lần
# đánh cược.
mcp>=1.12,<2

# Test giao kèo registry <-> MCP server (tests/test_mcp_contract.py). Nằm trong
# requirements chính vì bộ test đó chạy TRONG container:
#   docker exec hirewise_mcp python -m pytest /app/tests/test_mcp_contract.py -q
pytest==8.3.3