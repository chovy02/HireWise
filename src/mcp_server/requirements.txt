# Deps cho MCP server. Image RIÊNG với backend để dùng pydantic mới (mcp cần >=2.7),
# nhưng vẫn import được app.* (mount code backend + PYTHONPATH=/app).
mcp

# Đủ để import app.models / app.database / app.services.ai_agent.agent_tools:
sqlalchemy==2.0.29
psycopg2-binary==2.9.9
groq==0.11.0
httpx==0.27.2          # groq 0.11 cần httpx<0.28
pymupdf==1.24.0
python-dotenv==1.0.1
pydantic[email]
